streamlit==1.29.0
python-dotenv==1.0.1

# Langchain & vector store
langchain==0.1.16
faiss-cpu==1.7.4
sentence-transformers==2.2.2

# For tokenizer/model compatibility
tokenizers==0.13.3
sentencepiece==0.1.99

# Optional but often needed for embeddings
transformers==4.39.3

# For handling Arrow table formats if needed (used by some loaders)
pyarrow==10.0.1
